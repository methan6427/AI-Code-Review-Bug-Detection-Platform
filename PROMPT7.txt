Implement lightweight onboarding and contextual help improvements for the AI Code Review Platform frontend.

Goal:
Make the app easier to understand for first-time users without adding a full tour system.

Tasks:
1. Add smart helper panels or hint cards where useful, especially on:
   - repositories page
   - scans page
   - auth page if helpful
2. Add concise explanations such as:
   - Install the GitHub App to enable automatic scans
   - Manual scans are useful for baseline analysis
   - GitHub push and PR scans appear automatically after webhook events
3. Add contextual helper text for:
   - GitHub App import
   - manual repository creation
   - scan source meanings
   - retry semantics if appropriate
4. Keep guidance concise and non-intrusive.
5. Make hints feel like professional product guidance, not tutorial clutter.

Constraints:
- No full onboarding framework
- Keep copy short and useful

At the end:
1. summarize added onboarding/help UX
2. list files changed
3. note any help content that should be backed by docs later