Implement a UX pass for empty, loading, and error states across the AI Code Review Platform frontend.

Goal:
Replace plain or weak states with polished, helpful product-quality states.

Tasks:
1. Add meaningful empty states for:
   - no repositories
   - no scans
   - no issues
   - no GitHub installation selected
2. Add skeleton loading states for major pages/sections where appropriate:
   - dashboard widgets
   - repositories
   - scans
   - issue lists
3. Improve error states with:
   - concise explanation
   - optional retry button
   - clear next action
4. Keep empty states aligned with the developer-tool dark aesthetic.
5. Use subtle icons/illustrations if appropriate, but keep it professional.

Constraints:
- Do not overcomplicate state logic
- Reuse shared UI where possible

At the end:
1. summarize added states
2. list files changed
3. note any areas still lacking proper async state handling